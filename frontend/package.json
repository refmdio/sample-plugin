{
  "name": "@refmdio/sample-plugin-frontend",
  "private": true,
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=browser --target=es2020 --outfile=dist/index.mjs"
  },
  "dependencies": {
    "@refmdio/plugin-sdk": "^0.1.4"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.9.2"
  }
}
