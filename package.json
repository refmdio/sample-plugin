{
  "name": "@refmdio/sample-plugin",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "build": "esbuild frontend/index.mjs --bundle --format=esm --platform=browser --target=es2020 --outfile=frontend/dist/index.mjs"
  },
  "dependencies": {
    "@refmdio/plugin-sdk": "^0.1.3"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
